import {useEffect, useState} from "react";


const ChipsButtonGroup = ({children,...other}) => {

    let [content, setContent] = useState([]);

    useEffect(() => {
        
    }, [])

  return (
      <div {...other}>

      </div>
  );
}

export default ChipsButtonGroup;